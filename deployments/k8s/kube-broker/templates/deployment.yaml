apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kube-broker.fullname" . }}
  labels:
    {{- include "kube-broker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kube-broker.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "kube-broker.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        {{- if .Values.cliStressApp.enabled }}
        # CLI-Go stress-test runner (replaces the functional test clients)
        - name: stress-test
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.cliStressApp.image }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: STRESS_BENCHMARK_ENABLED
              value: "true"
            - name: STRESS_CONCURRENT
              value: {{ .Values.cliStressApp.concurrent | quote }}
            - name: STRESS_COUNT
              value: {{ .Values.cliStressApp.num | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- include "kube-broker.volumeMounts" . | nindent 12 }}
        {{- else }}
        # CLI-test apps (functional test clients)
        {{- range .Values.cliTestApps }}
        - name: {{ .name }}
          securityContext:
            {{- toYaml $.Values.securityContext | nindent 12 }}
          image: "{{ .image }}:{{ $.Values.imageTag }}"
          imagePullPolicy: {{ $.Values.imagePullPolicy }}
          {{- with .args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml $.Values.resources | nindent 12 }}
          volumeMounts:
            {{- include "kube-broker.volumeMounts" $ | nindent 12 }}
        {{- end }}
        {{- end }}

        # Crypto-broker server (the broker)
        - name: {{ .Values.server.name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.server.image }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "kube-broker.fullname" . }}-serverconfig
          env:
            - name: GODEBUG
              value: {{ .Values.server.godebug | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- include "kube-broker.volumeMounts" . | nindent 12 }}
      volumes:
        {{- include "kube-broker.volumes" . | nindent 8 }}
